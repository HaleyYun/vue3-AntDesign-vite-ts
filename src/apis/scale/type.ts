export interface RestliData {
    code: number
    message: string
    data: string
}

export interface Ad8SubmitType {
    answerList: string[],
    archiveNum: string
    serviceCode: string
    status: number

}